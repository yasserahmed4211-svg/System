/**
 * @deprecated Use 'src/app/core/guards/auth.guard' directly.
 * This file is a compatibility re-export.
 * The canonical guards (authGuard, authGuardChild) are functional CanActivateFn guards in core/guards/.
 */
export { authGuard, authGuardChild } from 'src/app/core/guards/auth.guard';
