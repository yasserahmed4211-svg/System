/**
 * Core Lookup Module — Barrel Export
 *
 * Public API for the lookup framework's core layer.
 * Exposes models and the data service only — no UI components.
 */
export * from './lookup.model';
export * from './lookup-data.service';
